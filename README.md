# 🏥 نظام إدارة عيادة التخسيس | Clinic Management System

## ✨ تم التحديث! نظام التقارير الجديد

تم إضافة نظام كامل لطباعة التقارير مع إمكانية تخصيص اسم العيادة!

---

## 🌐 روابط المشروع

- **GitHub Repository**: https://github.com/Faresbasem/pharm
- **التطبيق المباشر**: قريباً على Cloudflare Pages

## 🔐 بيانات تسجيل الدخول

### حساب المدير (Admin)
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`
- **الصلاحيات**: وصول كامل لجميع الوظائف

### حساب الطبيب (Doctor)  
- **اسم المستخدم**: `doctor`
- **كلمة المرور**: `doctor123`
- **الصلاحيات**: إدارة المرضى والقياسات فقط

---

## 🆕 الميزات الجديدة - نظام التقارير

### ✅ ما تم إضافته:

1. **طباعة تقرير شامل للمريض**
   - عرض كامل لبيانات المريض
   - إحصائيات التقدم (التغير في الوزن، الدهون، العضلات)
   - جدول كامل بتاريخ جميع القياسات
   - تصميم احترافي جاهز للطباعة

2. **تخصيص اسم العيادة** 
   - يمكن للـ Admin تغيير اسم العيادة من قاعدة البيانات
   - يظهر الاسم المخصص في جميع التقارير
   - يتم حفظ الإعدادات في جدول settings

3. **واجهة طباعة احترافية**
   - CSS خاص للطباعة (@media print)
   - إخفاء العناصر غير الضرورية عند الطباعة
   - تنسيق مثالي للطباعة على A4

---

## 🚀 كيفية استخدام نظام التقارير

### للمستخدمين العاديين:

1. اذهب إلى صفحة "المرضى"
2. اضغط على أيقونة 📄 بجانب اسم أي مريض
3. سيفتح التقرير الكامل بكل البيانات
4. اضغط زر "طباعة" أو `Ctrl+P`
5. اختر الطابعة أو احفظ كـ PDF

### للـ Admin - تغيير اسم العيادة:

يمكن تغيير اسم العيادة بطريقتين:

#### الطريقة 1: من قاعدة البيانات مباشرة
```bash
# تشغيل console قاعدة البيانات
npm run db:console:local

# إدخال الأمر التالي
INSERT OR REPLACE INTO settings (setting_key, setting_value, setting_type) 
VALUES ('clinic_name', 'اسم عيادتك هنا', 'string');
```

#### الطريقة 2: من كود الإعدادات
سيتم إضافة واجهة مستقبلاً للتحكم الكامل من لوحة الإعدادات.

---

## 📁 ملفات نظام التقارير

تم إضافة الملفات التالية:

1. **REPORT_SYSTEM_ADDON.js**
   - كود JavaScript كامل لنظام التقارير
   - يمكن إضافته إلى `public/static/app.js`
   - يحتوي على:
     - صفحة التقرير `PatientReportPageSimple()`
     - دالة تحميل التقرير `loadSimpleReport()`
     - CSS للطباعة
     - دعم تخصيص اسم العيادة

2. **add_reports.sh**
   - سكريبت bash لإضافة نظام التقارير تلقائياً
   - يضيف endpoints للـ backend
   - يضيف translations للـ frontend

---

## 🛠️ التقنيات المستخدمة

### Backend
- **Hono** v4.11.8 - إطار عمل سريع وخفيف
- **Cloudflare D1** - قاعدة بيانات SQLite موزعة
- **Cloudflare Pages** - استضافة Edge Computing
- **TypeScript** - لغة مكتوبة آمنة

### Frontend
- **Vanilla JavaScript** - بدون مكتبات ثقيلة
- **Tailwind CSS** - تصميم سريع وجميل
- **Font Awesome** - أيقونات احترافية

### Database
- **SQLite (D1)** - قاعدة بيانات محلية وسحابية
- **5 جداول**: Patients, Measurements, Users, Settings, Field_Settings

---

## 📊 قاعدة البيانات

### جدول Settings (للإعدادات)

```sql
CREATE TABLE settings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  setting_key TEXT UNIQUE NOT NULL,
  setting_value TEXT,
  setting_type TEXT DEFAULT 'string',
  description TEXT,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### الإعدادات الافتراضية:

| setting_key | setting_value | الوصف |
|------------|--------------|-------|
| clinic_name | نظام إدارة عيادة التخسيس | اسم العيادة |
| language | ar | اللغة الافتراضية |
| report_header | | رأس التقرير المخصص |
| report_footer | | ذيل التقرير المخصص |

---

## 🔧 التطوير المحلي

### المتطلبات
- Node.js 18+
- npm

### خطوات التشغيل

```bash
# 1. استنساخ المشروع
git clone https://github.com/Faresbasem/pharm.git
cd pharm

# 2. تثبيت التبعيات
npm install

# 3. إعداد قاعدة البيانات
npm run db:migrate:local
npm run db:seed

# 4. بناء المشروع
npm run build

# 5. تشغيل التطبيق
pm2 start ecosystem.config.cjs

# 6. فتح المتصفح
# http://localhost:3000
```

### أوامر مفيدة

```bash
# عرض حالة التطبيق
pm2 list

# عرض السجلات
pm2 logs clinic-webapp --nostream

# إعادة تعيين قاعدة البيانات
npm run db:reset

# تشغيل console قاعدة البيانات
npm run db:console:local
```

---

## 📋 السكربتات المتاحة

```json
{
  "dev": "vite",
  "dev:sandbox": "wrangler pages dev dist --d1=clinic-db --local --ip 0.0.0.0 --port 3000",
  "build": "vite build",
  "deploy": "npm run build && wrangler pages deploy dist --project-name webapp",
  "db:migrate:local": "wrangler d1 migrations apply clinic-db --local",
  "db:seed": "wrangler d1 execute clinic-db --local --file=./seed.sql",
  "db:reset": "rm -rf .wrangler/state/v3/d1 && npm run db:migrate:local && npm run db:seed",
  "db:console:local": "wrangler d1 execute clinic-db --local",
  "clean-port": "fuser -k 3000/tcp 2>/dev/null || true"
}
```

---

## ✨ الميزات الرئيسية

### 1. إدارة المرضى
- ✅ إضافة/تعديل/حذف مريض
- ✅ كود تلقائي (P001, P002, ...)
- ✅ بحث متقدم (اسم، كود، هاتف)
- ✅ حفظ جميع البيانات (اسم، سن، أمراض، أدوية، ملاحظات)

### 2. نظام القياسات
- ✅ إضافة قياسات جديدة لكل زيارة
- ✅ سجل تاريخي كامل
- ✅ تعديل/حذف القياسات
- ✅ جميع القياسات: وزن، دهون، عضلات، ماء، BMR، BMI، FFM

### 3. نظام المصادقة
- ✅ تسجيل دخول آمن
- ✅ نوعان من المستخدمين: Admin و User
- ✅ حماية المسارات والـ API

### 4. نظام التقارير (جديد!)
- ✅ طباعة تقرير شامل لكل مريض
- ✅ إحصائيات وتحليلات
- ✅ تخصيص اسم العيادة
- ✅ تصميم احترافي للطباعة

### 5. لوحة التحكم
- ✅ إحصائيات حية
- ✅ عرض آخر المرضى
- ✅ روابط سريعة

### 6. دعم متعدد اللغات
- ✅ عربي (افتراضي)
- ✅ إنجليزي
- ✅ قابل للتوسع

---

## 📖 التوثيق الإضافي

- **README.md** - هذا الملف
- **CUSTOMIZATION.md** - دليل التخصيص للمطورين
- **PROJECT_SUMMARY.md** - ملخص المشروع
- **QUICKSTART.md** - دليل البدء السريع
- **REPORT_SYSTEM_ADDON.js** - كود نظام التقارير

---

## 🤝 المساهمة

نرحب بالمساهمات! إذا كان لديك اقتراحات أو تحسينات:

1. Fork المشروع
2. أنشئ branch جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للـ branch (`git push origin feature/AmazingFeature`)
5. افتح Pull Request

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## 👨‍💻 المطور

تم التطوير بواسطة: **Fares Basem**

GitHub: [@Faresbasem](https://github.com/Faresbasem)

---

## 🎯 الإصدار

**النسخة**: 1.1.0 (مع نظام التقارير)  
**آخر تحديث**: 7 فبراير 2026  
**الحالة**: ✅ جاهز للاستخدام

---

## ⭐ إذا أعجبك المشروع

إذا وجدت هذا المشروع مفيداً، يرجى:
- ⭐ إضافة Star على GitHub
- 🔄 مشاركة المشروع
- 🐛 الإبلاغ عن أي مشاكل

---

**شكراً لاستخدامك نظام إدارة عيادة التخسيس!** 🚀
